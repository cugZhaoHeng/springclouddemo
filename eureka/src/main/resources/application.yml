server:
  port: 9999

spring:
  application:
    name: quantity-eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka
  server:
    eviction-interval-timer-in-ms: 500
    enable-self-preservation: false
